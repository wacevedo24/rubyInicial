<div class="container" >
  <% flash.each do |key, value| %>
    <div class="alert alert-<%= key %>">
      <%= value %>
    </div>
  <% end %>

  <h1> <%= @animal.nombre %> </h1>
  <%= image_tag @animal.url_foto %>
<h3>Editar </h3>
<%= render 'form' %>
</div>

